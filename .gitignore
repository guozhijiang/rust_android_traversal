# 构建产物
/target

# 由 scripts/setup-cargo-config.* 生成本地配置（含本机 NDK 绝对路径，不入库）
/.cargo/config.toml

# 运行产出（会话、截图、报告）
/sessions

# 用例执行产出（`script` 子命令默认落这里）
/reports

# 本地工具数据
/.workbuddy

# 杂项
*.log
*.tmp
